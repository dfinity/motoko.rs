use std::str::FromStr;
//use std::collections::HashMap;
use crate::ast::{Id, Exp_ as Exp}; // Maybe we could use `type Exp_ = Box<Exp>; enum Exp = { ... }` to simplify imports?

grammar;

pub Exp: Exp = {
    "_?_" => Exp::Hole
};

pub Num: i32 = {
    r"[0-9]+" => i32::from_str(<>).unwrap()
};

pub Id: Id = {
    r"[a-zA-Z][a-zA-Z_0-9]*" => String::from_str(<>).unwrap()
};

